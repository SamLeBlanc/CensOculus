// for future use

// const addTag = () => {
//   let id = $('#lens-name').text()
//   taggedDistricts[id] = {
//     id: id,
//     geoids: Object.values(heldDistricts).map(h => h.GEOID10),
//     names: Object.values(heldDistricts).map(h => h.NAME10),
//   }
//   dispayTags();
// }
//
// const dispayTags = () => {
//   $('#tag-text').text("");
//   Object.keys(taggedDistricts).forEach(t => {
//     $('#tag-text').append(`${taggedDistricts[t].id}<br>`)
//   })
// }
